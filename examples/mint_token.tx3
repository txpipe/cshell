party Minter;

tx mint_token (
  token_policy: Bytes,
  token_name: Bytes,
  quantity: Int,
) {

  locals {
    token: AnyAsset(token_policy, token_name, quantity),
  }

  mint {
    amount: token,
    redeemer: (),
  }

  input source {
    from: Minter,
    min_amount: fees,
  }

  output destination {
    to: Minter,
    amount: token - fees,
  }
}